export const Book = ({title}) => {
  // will have book prop
  //  book has properties id, title, status, photo url (can come in later)
  // have dropdown element/component
  return (
      <p>{title}</p>
  )
}
